########################################
#    Author: Zhengyi Peng (zpeng3)    #
########################################

Supported Platforms:
	+ Darwin (Mac)
	+ Linux

Prerequiesites:
	+ python3
	+ pip3

Run
		./cs132_twitterfeed
		Wait for the line "Server is listening to <http://localhost:8081/feed/start>
		You can now send GET requests to <http://localhost:8081/feed/start>

Stop:
	Type CTRL-C to stop the server

Troubleshotting:
	if the server cannot start because "address is in use", wait some time and start the server again, you can also use `kill $(lsof -t -i:8081)` to forcibly quit the process
