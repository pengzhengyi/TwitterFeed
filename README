Supported Platforms:
	+ Darwin (Mac)
	+ Linux

Prerequiesites:
	+ python3
	+ pip3

Run:
		./serve (this script will check for dependencies and start the local server)
		Server will respond to "http://localhost:8080/feed/start"

Stop:
	Type two CTRL-C (the first will stop pulling tweets, the second will stop the server)
