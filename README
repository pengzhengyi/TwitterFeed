Supported Platforms:
	+ Darwin (Mac)
	+ Linux

Prerequiesites:
	+ python3
	+ pip3

Run
		./serve (this script will check for dependencies and start the local server)
		Wait for the line "Server is listening to <http://localhost:8080/feed/start>
		You can now send GET requests

Stop:
	Type CTRL-C to stop the server
